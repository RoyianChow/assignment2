<div class="container">
    <h1><%=title %></h1>
    <a href="/contact-add" class="btn btn-primary" role="button">
        <i class="fas fa-plus-circle"></i> Add a Contact
    </a>
    <div class="row">
        <div class="column">
            <table class="table table-striped table-bordered table-hover">
                <thead class="table-dark">
                    <tr>
                        <th scope="col" class="text-center">#</th>
                        <th scope="col">Name</th>
                        <th scope="col">Email</th>
                        <th scope="col">company</th>
                        <th scope="col">address</th>
                        <th scope="col">number</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="contactList">
                    <% for(let count=0; count < contact.length; count++) { %>                   
                        <tr>
                            <td scope="row" class="text-center"></td>
                            <td><%= contact[count].name %></td>
                            <td><%= contact[count].email %></td>
                            <td><%= contact[count].company %></td>
                            <td><%= contact[count].address %></td>
                            <td><%= contact[count].number %></td>
                            <td class="text-center col-1">
                                <a class="btn btn-primary" href="/contact-edit/<%= contact[count].id %>">
                                    Edit
                                </a>
                            </td>
                            <td class="text-center col-1">
                                <a class="btn btn-warning" href="/contact-delete/<%= contact[count].id %>">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>